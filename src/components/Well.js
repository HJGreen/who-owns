import styled from 'styled-components';

const Well = styled.section`
  box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.08), 0px 15px 35px rgba(50, 50, 93, 0.12);
  border-radius: 4px;
  background-color: #fff;
  overflow: hidden;
`;

export default Well;